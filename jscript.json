// --- Mèo ---
const cat = document.getElementById("cat");
const bubble = document.getElementById("cat-talk");
let isBubbleVisible = false;

cat.addEventListener("click", () => {
  if (!isBubbleVisible) {
    bubble.classList.add("show");
    bubble.textContent = "Meowww I miss you!";
    isBubbleVisible = true;
  }
});

document.addEventListener("click", (event) => {
  if (!cat.contains(event.target) && !bubble.contains(event.target)) {
    bubble.classList.remove("show");
    isBubbleVisible = false;
  }
});

bubble.addEventListener("click", (event) => {
  event.stopPropagation();
});

// --- Thỏ ---
const rabbit = document.getElementById("rabbit");
rabbit.addEventListener("click", () => {
  rabbit.classList.add("hopping");
  rabbit.addEventListener("animationend", () => {
    rabbit.classList.remove("hopping");
  });
});


const overlay = document.getElementById("overlay");

function startGame() {
  overlay.classList.add("active"); // chỉ show overlay
}

function closeOverlay() {
  overlay.classList.remove("active"); // chỉ hide overlay
}

overlay.addEventListener("click", function (e) {
  if (e.target === overlay) {
    closeOverlay();
  }
});

// Mô phỏng hiệu ứng gõ tin nhắn
function typeMessage(message, className) {
  let chatBody = document.getElementById('chatBody');
  let messageDiv = document.createElement('div'); // Tạo một phần tử mới cho tin nhắn
  messageDiv.classList.add('chat-message', className);
   // Thêm class cho tin nhắn
  chatBody.appendChild(messageDiv); // Thêm vào chatBody
  let i = 0;
  let interval = setInterval(() => {
    messageDiv.innerHTML += message.charAt(i);  // Hiển thị từng ký tự
    chatBody.scrollTop = chatBody.scrollHeight; // Cuộn xuống cuối cùng
    i++;
    if (i === message.length) {
      clearInterval(interval); // Dừng khi gõ xong
      showOptions(); // Hiển thị các lựa chọn
    }
  }, 50); // Điều chỉnh tốc độ gõ
}

// Hiển thị các lựa chọn (ucii, hongg, được)
function showOptions() {
  document.getElementById('chatOptions').style.display = 'flex';
}

function sendOptionMessage1() {
    document.getElementById('chatOptions').style.display = 'none';

    let chatBody = document.getElementById('chatBody');
    chatBody.innerHTML += `
        <div class="chat-message from-me">Tớ rùii</div>
    `;
    chatBody.scrollTop = chatBody.scrollHeight; 
    setTimeout(() => {
        chatBody.innerHTML += `
            <div class="chat-message from-them">Tụi tớ xin cãm ưn cậu nhìu nhoaa</div>
        `;
        chatBody.scrollTop = chatBody.scrollHeight;
        setTimeout(() => {    
            chatBody.innerHTML += `
                <div class="chat-message from-them">Tụi tớ xin tặng cậu 1 lượt quay siu siu may mắn</div>
            `;
            chatBody.scrollTop = chatBody.scrollHeight; 
            setTimeout(() => {
                // Tin nhắn thứ tư
                chatBody.innerHTML += `
                    <div class="chat-message from-them">Hoặc hong =))</div>
                `;
                chatBody.scrollTop = chatBody.scrollHeight; 
            }, 3000); 

        }, 1900); 
    }, 400); 
}


function sendOptionMessage2() {
    document.getElementById('chatOptions').style.display = 'none';

    let chatBody = document.getElementById('chatBody');
    chatBody.innerHTML += `
        <div class="chat-message from-me">Tớ chưaaa</div>
    `;
    chatBody.scrollTop = chatBody.scrollHeight; 
    setTimeout(() => {
        chatBody.innerHTML += `
            <div class="chat-message from-them">Uii, tại sao cậu chưa thử đii, ngon lắm lun óo 🥺</div>
        `;
        chatBody.scrollTop = chatBody.scrollHeight;
        setTimeout(() => {    
            chatBody.innerHTML += `
                <div class="chat-message from-them">Hay cậu hong mua được hủm, gửi stk tớ ting ting cho mà thử</div>
            `;
            chatBody.scrollTop = chatBody.scrollHeight; 
            setTimeout(() => {

                chatBody.innerHTML += `
                    <div class="chat-message from-them">Cậu inbox stk qua page ngay nhó </div>
                `;
                chatBody.scrollTop = chatBody.scrollHeight; // Cuộn xuống dưới
                  setTimeout(() => {
                    chatBody.innerHTML += `
                    <div class="chat-message from-them">Đừng để tớ phải gia trưởng 🙄</div>
                `;
                chatBody.scrollTop = chatBody.scrollHeight;
                                  }, 2000);
            }, 2000); 

        }, 2000); 
    }, 1000); 
}


function sendOptionMessage3() {
    let chatBody = document.getElementById('chatBody');
    chatBody.innerHTML += `
        <div class="chat-message from-me">Hehehehe</div>
    `;
    setTimeout(() => {
        typeMessage("Được rồi! Cậu quay giúp đi.", 'from-them');
    }, 500);
    

    document.getElementById('chatOptions').style.display = 'none';
}

window.onload = () => {
  let chatBody = document.getElementById('chatBody');
  setTimeout(() => {
      chatBody.innerHTML += `
        <div class="chat-message from-them">Heluu, cho tớ hỏi cậu đã thưởng thức sữa chua của tụi tớ chưa ạaa</div>
    `;
}, 1000);
};

const wheel = document.getElementById('wheel');
const segments = document.querySelectorAll('.segment');
const SEG_COUNT = segments.length;
let currentRotation = 0;
let isSpinning = false;

function spinWheel() {
  if (!wheel) return;
  if (isSpinning) return;
  isSpinning = true;

  // quay thêm 5..8 vòng + góc random 0..359
  const extra = Math.floor(Math.random() * 360) + 360 * (5 + Math.floor(Math.random() * 4));
  currentRotation += extra;

  wheel.style.transition = 'transform 4.5s cubic-bezier(.22,.9,.12,1)';
  wheel.style.transform = `rotate(${currentRotation}deg)`;

  wheel.addEventListener('transitionend', onSpinEnd, { once: true });
}

function onSpinEnd() {
  const finalDeg = currentRotation % 360;
  const segAngle = 360 / SEG_COUNT;


  let angleAtTop = (360 - finalDeg) % 360;
  let index = Math.floor(angleAtTop / segAngle);
  index = (index + SEG_COUNT) % SEG_COUNT;

  const chosen = segments[index] ? segments[index].textContent.trim() : '???';

  setTimeout(() => {
    alert(`Bạn trúng: ${chosen}`);
    isSpinning = false;
  }, 200);
}
